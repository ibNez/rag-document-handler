version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      MILVUS_HOST: milvus
      MILVUS_PORT: 19530
    depends_on:
      - milvus
  milvus:
    image: milvusdb/milvus:latest
    ports:
      - "19530:19530"
